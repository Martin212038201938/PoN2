# Development (local backend)
VITE_API_URL=http://localhost:8081/api

# Production with reverse proxy (recommended)
# Leave unset - frontend will use relative /api path

# Production with separate API domain
# VITE_API_URL=https://api.pon2.yellow-plane.com/api
